load("@rules_rust//rust:defs.bzl", "rust_library", "rust_test")

rust_library(
    name = "kv",
    crate_name = "giztoy_kv",
    srcs = glob(["src/**/*.rs"]),
    visibility = ["//visibility:public"],
    deps = [
        "@crate_index//:redb",
        "@crate_index//:thiserror",
    ],
)

rust_test(
    name = "kv_test",
    crate = ":kv",
    deps = [
        "@crate_index//:tempfile",
    ],
)
