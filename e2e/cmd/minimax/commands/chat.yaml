# Example: Text chat request
# Usage: minimax -c myctx text chat -f chat.yaml

model: MiniMax-M2.1
messages:
  - role: system
    content: You are a helpful assistant.
  - role: user
    content: Hello, who are you?
max_tokens: 1000
temperature: 0.7
