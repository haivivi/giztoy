# Test: genx list filtered by generator type
steps:
  - args: [ctx, add, dev]
  - args: [ctx, use, dev]
  - args: [genx, add, testdata/models/generator-qwen.json]
  - args: [genx, add, testdata/models/tts-minimax.json]
  - args: [genx, list, --type, generator]
    exit_code: 0
    stdout_contains: "generator"
    stdout_contains: "qwen"
