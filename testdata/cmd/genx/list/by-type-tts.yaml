# Test: genx list filtered by tts type
steps:
  - args: [ctx, add, dev]
  - args: [ctx, use, dev]
  - args: [genx, add, testdata/models/generator-qwen.json]
  - args: [genx, add, testdata/models/tts-minimax.json]
  - args: [genx, list, --type, tts]
    exit_code: 0
    stdout_contains: "tts"
    stdout_contains: "minimax"
