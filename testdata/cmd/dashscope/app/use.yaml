steps:
  - args: [ctx, add, dev]
  - args: [ctx, use, dev]
  - args: [dashscope, app, add, default, --api-key, k1]
  - args: [dashscope, app, use, default]
    exit_code: 0
    stdout_contains: "Switched"
