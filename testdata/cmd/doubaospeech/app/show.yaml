steps:
  - args: [ctx, add, dev]
  - args: [ctx, use, dev]
  - args: [doubaospeech, app, add, test, --app-id, "111", --token, "tok"]
  - args: [doubaospeech, app, show, test, --json]
    exit_code: 0
    stdout_json:
      app-id: "111"
      token: "tok"
