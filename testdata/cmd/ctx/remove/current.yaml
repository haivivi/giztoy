# Test: ctx remove refuses to delete current context
steps:
  - args: [ctx, add, dev]
    exit_code: 0
  - args: [ctx, use, dev]
    exit_code: 0
  - args: [ctx, remove, dev]
    exit_code: 1
    stderr_contains: "cannot remove current"
