# Test: ctx config set rejects unknown key
steps:
  - args: [ctx, add, dev]
    exit_code: 0
  - args: [ctx, use, dev]
    exit_code: 0
  - args: [ctx, config, set, foo, bar]
    exit_code: 1
    stderr_contains: "unknown key"
