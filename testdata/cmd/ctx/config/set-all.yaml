# Test: ctx config set all 4 keys
steps:
  - args: [ctx, add, dev]
    exit_code: 0
  - args: [ctx, use, dev]
    exit_code: 0
  - args: [ctx, config, set, kv, "badger:///data"]
    exit_code: 0
  - args: [ctx, config, set, storage, "local:///files"]
    exit_code: 0
  - args: [ctx, config, set, vecstore, "hnsw:///idx"]
    exit_code: 0
  - args: [ctx, config, set, embed, "dashscope://sk-xxx"]
    exit_code: 0
  - args: [ctx, show, --json]
    exit_code: 0
    stdout_json:
      name: dev
      kv: "badger:///data"
      storage: "local:///files"
      vecstore: "hnsw:///idx"
      embed: "dashscope://sk-xxx"
