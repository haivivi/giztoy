# Shared test data for matchtest benchmark tool.
# Used by both Go and Rust implementations.

# Test rules (intent patterns)
filegroup(
    name = "rules",
    srcs = glob(["rules/**"]),
    visibility = ["//visibility:public"],
)
