# Build options
build --enable_bzlmod

# Test options
test --test_output=errors

# Cache
build --disk_cache=~/.cache/bazel

# Platform
build --incompatible_enable_cc_toolchain_resolution

# CC toolchains:
# - macOS: System clang (requires PATH)
# - Linux: Zig CC (hermetic, downloaded)

# PATH for system clang on macOS (needed for Rust linking)
build --action_env=PATH=/usr/bin:/bin:/usr/sbin:/sbin:/opt/homebrew/bin:/usr/local/bin
build --host_action_env=PATH=/usr/bin:/bin:/usr/sbin:/sbin:/opt/homebrew/bin:/usr/local/bin
